{
  "name": "fconline",
  "private": true,
  "workspaces": ["apps/*", "packages/*"],
  "packageManager": "npm@11.8.0",
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint"
  },
  "devDependencies": {
    "turbo": "^2.9.1",
    "expo-router": "~6.0.23",
    "babel-preset-expo": "~14.0.0"
  }
}
